/** Configuration constants for Copilot Insights. */
/**
 * Default cost per seat, used only as a fallback when useOrgConfig is not
 * available. Prefer the `costPerSeat` value from the `useOrgConfig` hook.
 */
export const COST_PER_SEAT_DEFAULT = 19;
