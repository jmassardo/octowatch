Todos
- Eliminiate minio as it's EOL
- Add metric for actions
    - list of workflows that always fail
    - list of workflows that always hit the timeout
- Comprehensive review: We need to perform a comprehensive code review of Octowatch as there are a number of variances in the UI elements and a number of things that don't appear to work.
    - Ensure all pages use consistent visual elements and styling
    - Ensure all pages use similar slideout pane on the right side instead of modals
    - Ensure all pages use consistent filtering controls and styles
    - Ensure all side panes show human friendly forms/tables instead of json.
    - ensure all data fields have valid real data instead of mockup data
    - ensure all pages load successfully
    - ensure all tables, chips, and other data fields actually show data. for example, the health/governance page shows all 0's.
    - ensure summary data chips are clickable to automatically filter and display the entries
    - Ensure data is coming from audit log events when possible.
    - ensure api polling is minimal. it's fine to call the api to enrich information when events happen, but we shouldn't poll for anything aside from the scheduled sync
    - ensure all tables have sortable and filterable columns
    - ensure all tables have clickable rows. either show more information about an event or take the user directly to the asset in question, i.e. on the top failing workflows table, the user should be able to click the row and get a list of the failed runs for that workflow. they should also be able to click on the workflow name or repo name and be taken to that page.
    - ensure all data controls have a help icon that provide the user more information about where the data comes from and how to interpert the data and what actions they should consider.
    - ensure any api call is aware of rate limits, conditional requests, and other github api best practices
    - ensure copilot data is fetched on a schedule using the daily usage api. the frontend shouldn't call the copilot endpoints at runtime, only during the sync.
- Review other aspects of the app to identify any other dead code, TODO's in code, functions that are only mocked but don't work, security vulnerabilities, or any other issues you see
- Api usage reduction. As a general rule, we should rely on audit log events as our primary data source. In circumstances where we need to enrich data or perform additional analysis, we should call the API in response to an event.
    - we should attempt to limit our polling to only the github sync and the copilot usage data syncs.
- Executive "Metrics that matter" on the dashboard. I want to start classifying some of the metrics into 3 categories:
    - Are we shipping faster?
    - Are we shipping safer?
    - Are we shipping cheaper?